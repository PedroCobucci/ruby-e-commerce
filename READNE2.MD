- Rode o comando para setar o script auxiliar:

nano .env

```
./rd setup
source ~/.bashrc
rd
````

docker build --build-arg UID=$UID -t rd-commerce .

docker compose build

rd run bundle install

rd run_test bundle install

rd db:setup

rd start

rd sidekiq 

